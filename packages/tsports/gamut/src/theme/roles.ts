/**
 * @tsports/gamut - Theme roles
 *
 * Ported from roles.go
 */

/**
 * Role is used for type-safety
 */
export enum Role {
  Foreground = 0,
  Background = 1,
  Base = 2,
  AlternateBase = 3,
  Text = 4,
  Selection = 5,
  Highlight = 6,
}
