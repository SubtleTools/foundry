/**
 * @tsports/gamut - Theme roles
 *
 * Ported from roles.go
 */

/**
 * Role is used for type-safety
 */
export enum Role {
  Foreground = 0,
  Background,
  Base,
  AlternateBase,
  Text,
  Selection,
  Highlight,
}
