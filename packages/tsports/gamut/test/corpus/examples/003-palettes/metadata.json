{
  "skip": true,
  "reason": "Uses unseeded RNG in generator functions - output is non-deterministic",
  "notes": "Multiple generator functions use K-means with random initialization. Would need seeded RNG in both Go and TS for deterministic comparison."
}
